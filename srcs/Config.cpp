#include "../includes/Config.hpp"

Config::Config(){

}

Config::~Config(){

}

int Config::parseFile(const std::string& filename) {
	//bonne merde pour le parsing :)
}

std::string Config::getError() {
	//pt faire un switch et des cases avec un int que tu renvoies pour les erreurs ? 
}
